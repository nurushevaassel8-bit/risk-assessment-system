# Инструкция по загрузке в GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите кнопку **"New repository"** (или **"+"** → **"New repository"**)
3. Заполните:
   - **Repository name**: `risk-assessment-system` (или другое название)
   - **Description**: "Система оценки рисков активов"
   - Выберите **Public** или **Private**
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
4. Нажмите **"Create repository"**

## Шаг 2: Инициализируйте Git в проекте

Откройте терминал в папке проекта и выполните:

```bash
# Инициализация Git репозитория
git init

# Добавление всех файлов (кроме тех, что в .gitignore)
git add .

# Первый коммит
git commit -m "Initial commit: Система оценки рисков с аутентификацией"
```

## Шаг 3: Подключите удаленный репозиторий

```bash
# Замените YOUR_USERNAME и YOUR_REPO на ваши данные
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPO.git
```

## Шаг 4: Загрузите код на GitHub

```bash
# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код
git push -u origin main
```

## Готово! 🎉

Ваш проект теперь на GitHub!

---

## Дополнительные команды

### Если нужно обновить код позже:

```bash
# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Загрузить на GitHub
git push
```

### Если нужно клонировать репозиторий:

```bash
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git
cd YOUR_REPO
```

### Просмотр статуса:

```bash
git status
```

---

## Что будет загружено

✅ **Будет загружено:**
- Весь исходный код (app.py, templates, скрипты)
- Документация (DOCUMENTATION/)
- requirements.txt
- README.md
- .gitignore

❌ **НЕ будет загружено** (благодаря .gitignore):
- База данных (risk_assessment.db)
- Статические изображения (heatmaps)
- Виртуальное окружение (venv/)
- Кэш Python (__pycache__/)
- Логи и временные файлы

---

## Рекомендации

1. **Измените пароль администратора** перед загрузкой в публичный репозиторий
2. **Проверьте .gitignore** - убедитесь, что чувствительные данные не попадут в репозиторий
3. **Добавьте описание** в настройках репозитория на GitHub
4. **Добавьте теги** (tags) для версий проекта

---

## Проблемы?

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
```

### Ошибка: "failed to push some refs"
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### Нужно обновить .gitignore после коммита
```bash
git rm -r --cached .
git add .
git commit -m "Update .gitignore"
```

